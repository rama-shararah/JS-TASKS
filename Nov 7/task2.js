function LargestNumber(x, y){	
    if((x >y)){
        document.write(`the two numbers are: ${x} and ${y}, and the largest number is ${x}`)
    }
    else { 
            document.write(`the two numbers are: ${x} and ${y}, and the largest number is ${y}`);
        }
        
    }
    LargestNumber(8,19);

    document.write( "<br>" );

    LargestNumber(99,70);
